@model IEnumerable<aspCoreEmpty.Models.TypesExtended>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
    <h4>List of types</h4>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NameCategory)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NameCategory)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idtypes">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idtypes">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idtypes">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
